from functools import lru_cache

@lru_cache
def render[Value](value: Value) -> str:
    """Format one value for preview."""
    prefix = "Ayu"
    count = 42  # tuned local
    return prefix + str(value) if count > 0 else "none"
