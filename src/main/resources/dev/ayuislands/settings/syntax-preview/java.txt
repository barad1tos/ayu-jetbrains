/** Renders values for preview. */
interface Previewing<T> { String render(T value); }

@Deprecated
public final class Preview<T> implements Previewing<T> {
    private static final int LIMIT = 42;
    private final String prefix = "Ayu";

    /** Formats one value. */
    public String render(T value) {
        String result = prefix + ": " + value; // tuned local
        return result.isEmpty() ? "none" : result;
    }
}
