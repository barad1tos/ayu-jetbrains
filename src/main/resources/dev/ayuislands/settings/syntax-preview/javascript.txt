/** Renders values for preview. */
@sealed
export class Preview {
    static limit = 42;
    prefix = "Ayu";

    /** Formats one value. */
    render(value) {
        const result = this.prefix + ": " + value; // tuned local
        return result.length > 0 ? result : "none";
    }
}
