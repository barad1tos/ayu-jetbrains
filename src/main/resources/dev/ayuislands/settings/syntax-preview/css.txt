@media (min-width: 42rem) {
    .preview-card[data-preview="ayu"] {
        --accent: #ffcc66;
        color: var(--accent);
        padding: 12px;
        background-image: url("preview.svg");
        /* tuned declaration */
    }
}
